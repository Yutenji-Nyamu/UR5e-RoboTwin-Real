diff --git a/policy/pi05/src/openpi/training/checkpoints.py b/policy/pi05/src/openpi/training/checkpoints.py
index f32a831..178611e 100644
--- a/policy/pi05/src/openpi/training/checkpoints.py
+++ b/policy/pi05/src/openpi/training/checkpoints.py
@@ -9,7 +9,6 @@ from typing import Protocol
 from etils import epath
 import jax
 import orbax.checkpoint as ocp
-import orbax.checkpoint.future as future
 
 from openpi.shared import array_typing as at
 import openpi.shared.normalize as _normalize
@@ -126,7 +125,10 @@ class CallbackHandler(ocp.AsyncCheckpointHandler):
             args.callback(directory)
 
     async def async_save(self, directory: epath.Path, args: CallbackSave) -> list[futures.Future]:
-        return [future.CommitFutureAwaitingContractedSignals(asyncio.to_thread(self.save, directory, args))]
+        # Orbax 0.11.1 has no CommitFutureAwaitingContractedSignals. Finish the
+        # small asset callback before returning; no background Future remains.
+        await asyncio.to_thread(self.save, directory, args)
+        return []
 
     def restore(self, *args, **kwargs):
         raise NotImplementedError("CallbackHandler does not support restore")
