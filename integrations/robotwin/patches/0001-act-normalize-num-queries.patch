diff --git a/policy/ACT/detr/models/detr_vae.py b/policy/ACT/detr/models/detr_vae.py
--- a/policy/ACT/detr/models/detr_vae.py
+++ b/policy/ACT/detr/models/detr_vae.py
@@ -54,6 +54,9 @@ class DETRVAE(nn.Module):
         hidden_dim = transformer.d_model
         self.action_head = nn.Linear(hidden_dim, state_dim)
         self.is_pad_head = nn.Linear(hidden_dim, 1)
+        # Some ACT argument paths provide this value as a one-element sequence.
+        if isinstance(num_queries, (tuple, list)):
+            num_queries = num_queries[0]
         self.query_embed = nn.Embedding(num_queries, hidden_dim)
         if backbones is not None:
             self.input_proj = nn.Conv2d(backbones[0].num_channels, hidden_dim, kernel_size=1)
